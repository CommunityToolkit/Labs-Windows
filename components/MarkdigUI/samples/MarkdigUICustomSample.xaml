<!--  Licensed to the .NET Foundation under one or more agreements. The .NET Foundation licenses this file to you under the MIT license. See the LICENSE file in the project root for more information.  -->
<Page x:Class="MarkdigUIExperiment.Samples.MarkdigUICustomSample"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:controls="using:CommunityToolkit.Labs.WinUI.MarkdigUI"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="using:MarkdigUIExperiment.Samples"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <controls:MarkdownViewer
            Grid.Column="0"
            Config="{x:Bind MarkdownConfig, Mode=OneWay}"/>
        <TextBox
            x:Name="MarkdownTextBox"
            Grid.Column="1"
            AcceptsReturn="True"
            TextChanged="TextBox_TextChanged"/>
    </Grid>
</Page>
