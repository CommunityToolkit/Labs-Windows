<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:compositioncollectionview="using:CommunityToolkit.Labs.WinUI.CompositionCollectionView"
                    xmlns:labs="using:CommunityToolkit.Labs.WinUI">

    <Style TargetType="compositioncollectionview:CompositionCollectionView">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="compositioncollectionview:CompositionCollectionView">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Canvas x:Name="contentPanel"
                                Background="Transparent" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
